# Normalise line endings so a Windows checkout and a Linux checkout produce
# byte-identical sources; Python does not care, but diffs and CI do.
* text=auto eol=lf

*.json text eol=lf
*.png binary
*.webp binary
*.zip binary
*.whl binary
*.gz binary
